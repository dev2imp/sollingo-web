# Initilize and push
git init
git add .
git commit -m "Initial commit: Sol Lingo web app (React + TypeScript)"

git remote add origin https://github.com/dev2imp/sol-lingo-web.git
git branch -M main
git push -u origin main

# Add Some Updates
git add .
git commit -m "Favorites were added"
git push